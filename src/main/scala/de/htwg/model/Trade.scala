package de.htwg.model;

class Trade {
  def tradeCall(p1: Player, p2: Player, wantMon: Int, /*wantProp: List[BoardField],*/ givesMon: Int/*, givesProp: List[BoardField]*/): Boolean = {
    false
  }
}
