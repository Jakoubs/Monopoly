package de.htwg.model

import org.scalatest.matchers.should.Matchers.be
import org.scalatest.wordspec.AnyWordSpec

class TradeSpec extends AnyWordSpec {
  "Trade" should {
    "be able to trade" in {
      val p1 = new Player("TestPlayer1")
      val p2 = new Player("TestPlayer2")
      val FieldForTradeP1 = new Field()
      val FieldForTradeP1 =
      val FieldList = BoardField();
      trade(p1,p2,100,)
    }

    }
}